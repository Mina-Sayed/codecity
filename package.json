{
  "name": "codecity",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run build && vitest --run",
    "analyzer": "turbo run build && node apps/analyzer/dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "24.13.5",
    "turbo": "2.10.13",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
