{
  "name": "codecity",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "vitest --run",
    "analyzer": "node --experimental-strip-types apps/analyzer/src/cli.ts"
  },
  "devDependencies": {
    "turbo": "2.10.13",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
